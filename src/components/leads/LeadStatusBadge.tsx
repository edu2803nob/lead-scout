/**
 * Kept as a thin alias so existing imports keep working.
 * The canonical implementation lives in the design system.
 */
export { StatusBadge as LeadStatusBadge } from "@/components/ds/StatusBadge";
